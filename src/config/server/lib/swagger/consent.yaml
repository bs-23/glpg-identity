paths:
    /api/consents:
        get:
            tags:
                - consents
            summary: Get all consents by country code and language code.
            parameters:
                - name: country
                  in: query
                  required: true
                  description: Country code for which consent is to be returned.
                  schema:
                    type: string
                - name: locale
                  in: query
                  required: false
                  description: Language code for which consent is to be returned.
                  schema:
                    type: string
            responses:
                200:
                    description: The request has succeeded.
                    content:
                        applicaiton/json:
                            schema:
                                type: object
                                properties:
                                    data:
                                        $ref: "#/components/schemas/consentsForCountry/properties/data"
                                    errors:
                                        $ref: "#/components/schemas/errorBodyInSuccess"
                400:
                    description: Invalid query parameter.
                401:
                    description: User is not authenticated.

components:
    schemas:
        consent:
            type: object
            properties:
                id:
                    type: string
                title:
                    type: string
                rich_text:
                    type: string
                slug:
                    type: string
                opt_type:
                    type: string
                category:
                    type: string
                category_title:
                    type: string
                country_iso2:
                    type: string
                purpose:
                    type: string
                language_code:
                    type: string
        consentsForCountry:
            type: object
            properties:
                data:
                    type: array
                    items:
                        $ref: "#/components/schemas/consent"
