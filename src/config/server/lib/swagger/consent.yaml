paths:
    /api/consents:
        get:
            tags:
                - consents
            summary: Get all consents by country iso2.
            parameters:
                - name: country_iso2
                  in: query
                  required: true
                  description: Country iso2 for which consent is to be returned.
                  schema:
                    type: string
            responses:
                200:
                    description: The request has succeeded.
                    content:
                        applicaiton/json:
                            schema:
                                $ref: "#/components/schemas/consentsForCountry"
                401:
                    description: User is not authenticated.

components:
    schemas:
        consent:
            type: object
            properties:
                id:
                    type: string
                title:
                    type: string
                slug:
                    type: string
                type:
                    type: string
                opt_type:
                    type: string
                category:
                    type: string
                category_title:
                    type: string
                country_iso2:
                    type: string
        consentsForCountry:
            type: object
            properties:
                country_iso2:
                    type: string
                consents:
                    type: array
                    items:
                        $ref: "#/components/schemas/consent"
